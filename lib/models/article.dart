class Article{
  final String articleImage, articleTitle, articleSubTitle, authorName, date, homeImage, homeTitle, text
  ,firstTitle, secondTitle, firstText, secondText, endText1, endText2;
  Article({this.articleImage, this.articleTitle, this.articleSubTitle, this.authorName,
    this.date, this.homeImage, this.homeTitle, this.text, this.firstTitle, this.secondTitle
    , this.firstText, this.secondText, this.endText1, this.endText2});
}